| # | Scenario Summary                          | Classification        | Explanation (≤3 sentences)                                                                                                                                                                     |
| - | ----------------------------------------- | --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | Search results displayed under heavy load | **Risk**              | Heavy load threatens the <1s latency ASR. No scalability mechanisms are defined in the architecture. User experience and performance may degrade.                                              |
| 2 | Account creation stored externally        | **Tradeoff Point**    | External storage improves interoperability but introduces performance dependency. Security and latency are affected simultaneously. Architectural decisions must balance speed vs integration. |
| 3 | Login loads account externally            | **Sensitivity Point** | Authentication performance depends on external system speed. Small delays strongly impact usability. External latency directly influences system responsiveness.                               |
| 4 | Invalid input validation error message    | **Non-Risk**          | Local validation aligns with existing architecture. Minimal complexity and low architectural impact. No conflicts with existing ASRs.                                                          |
| 5 | Server saves item and confirms to user    | **Risk**              | Synchronous server communication may violate latency goals. Reliability and availability requirements are not defined. Performance degradation could occur during peak usage.                  |
| 6 | External systems push updates             | **Sensitivity Point** | Interoperability depends on synchronization timing. Architectural changes in external integration heavily affect consistency. Tight coupling increases system fragility.                       |
| 7 | Concurrent edits on same data             | **Tradeoff Point**    | Concurrency control balances performance and data integrity. Current ASRs lack conflict resolution requirements. Design choices affect latency and reliability.                                |
